# classicmice

1) install Node.js (v18+ recommended)
2) `cd server`
3) `npm install`
4) copy `.env.example` to `.env` and set DB credentials/ports
5) run migrations: `npm run migrate`
6) start dev: `npm run dev` (tsx) or build+run: `npm run build && npm start`